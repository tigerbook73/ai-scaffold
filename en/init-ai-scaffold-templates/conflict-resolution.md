# Existing Files/Directories Detected, Initialization Paused

The following paths conflict with scaffold target paths:

| Path | Type | Suggested handling |
| ---- | ---- | ------------------ |
| CLAUDE.md | file | Back up as CLAUDE.md.bak and overwrite / skip / merge manually |
| docs/conventions/ | directory | Inspect internal files and decide individually |
| apps/web/CLAUDE.md | file (package-level) | Back up and overwrite / skip / merge manually |

Choose how to proceed:

1. **Decide individually** (default): confirm each conflicting path one by one
2. **Back up all and continue**: back up all conflicting files as `.bak`, then overwrite automatically
3. **Skip all**: keep all existing files and only generate missing files

Reply with your choice (1 / 2 / 3), then initialization will continue.
