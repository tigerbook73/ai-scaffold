{
  "name": "test-review-gate",
  "description": "在测试文件上强制执行 @reviewed-by 注释和 @cases / it() 名称一致性约束，通过 pre-commit hook 和 Claude 规则实现",
  "dependencies": [],
  "components": {
    "rules": [
      {
        "name": "test-review-gate",
        "file": "rules/test-review-gate.md"
      }
    ],
    "scripts": [
      {
        "name": "check-test-cases",
        "file": "scripts/check-test-cases.ts"
      },
      {
        "name": "check-test-review",
        "file": "scripts/check-test-review.ts",
        "hook": "pre-commit"
      }
    ]
  }
}
