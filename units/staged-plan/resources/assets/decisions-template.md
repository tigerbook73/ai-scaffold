# <feature 名称> 全局约束 / 决策清单

> 只放对多个步骤或章节有约束力的、已经拍板的决定。每个细化步骤开始前都会完整读这份文件。
> 保持精简——某一步骤内部的细节决定放在对应步骤里，不要塞进来。
>
> [arch] 标记：如果这条决定通用性足够强、适用范围超出本 feature，打此标记。
> feature 完成时归并到架构指导文档。标记要当场打，不要等做完再回忆。

---

## 数据层

<!-- 示例：用户表主键统一用 UUID。（来源：Step 2 数据层实现，理由：跨服务引用时不暴露自增 ID） -->
<!-- 示例：[arch] 所有软删除字段统一命名 deleted_at，类型 timestamp nullable。（来源：Step 2） -->

## API 层

<!-- 示例：所有 API 错误返回统一用 { code: string, message: string } 结构。（来源：架构阶段） -->

## 前端

## 其他横切约束
