pre-commit:
  commands:
    lint:
      glob: "*.{ts,js}"
      run: pnpm exec eslint --fix {staged_files} && pnpm exec prettier --write {staged_files}
      stage_fixed: true
    format:
      glob: "*.{md,json,yaml,yml}"
      run: pnpm exec prettier --write {staged_files}
      stage_fixed: true
    # aisk-test-review-gate-check-reviewed-by-commit-marker:
    #   run: bun .aisk/test-review-gate/scripts/check-reviewed-by-commit-marker.js {staged_files}
    # aisk-test-review-gate-check-test-cases-match-it:
    #   run: bun .aisk/test-review-gate/scripts/check-test-cases-match-it.js {staged_files}
